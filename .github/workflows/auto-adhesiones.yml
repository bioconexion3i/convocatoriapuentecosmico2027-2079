name: Auto-Adhesiones
on:
  issues:
    types: [labeled]
jobs:
  add-adhesion:
    runs-on: ubuntu-latest
    if: github.event.label.name == 'adhesion-aprobada'
    steps:
    - uses: actions/checkout@v4
    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        node-version
